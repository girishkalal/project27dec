//creating the Shedule class to Shedule the batchclass
Global class keyContactShedule implements Schedulable {
    global void execute(SchedulableContext SC) {
      KeyContactBatch M = new KeyContactBatch(); 
    
   }
}